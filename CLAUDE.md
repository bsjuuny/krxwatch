# 🚀 KRX Watch (물리 엔진 주식판) 개발자 가이드

안녕하세요! 이 프로젝트는 주식과 원자재 가격 변동을 딱딱한 도표가 아니라, **물리 엔진**을 사용해 중력과 충돌이 느껴지는 실시간 대시보드로 표현한 실험적인 서비스입니다.

---

## 🏗 시스템 작동 방식 (The Fun Part!)

1.  **물리 세계 생성**: `Matter.js` 엔진을 사용하여 중력이 흐르는 가상의 '그릇'을 만듭니다.
2.  **데이터 바디(Body) 생성**: 금 시세나 유가 정보가 들어오면, 그 결과에 따라 동그란 '공'이나 네모난 '카드' 형태의 물리 객체를 생성해 그릇에 던져 넣습니다.
3.  **인터랙션**: 사용자는 마우스로 이 가격 카드들을 집어서 던지거나 흔들 수 있습니다.

---

## 🛠 주요 명령어 및 개발 안내

- `npm run dev`: 물리 엔진이 돌아가는 캔버스 화면을 수정하거나 속도/중력 수치를 조정할 때 사용하세요.
- `npm run build`: 브라우저 성능에 웹이 끊기지 않도록 최적화해서 빌드합니다.

---

## 💡 개발자를 위한 팁

### 물리 엔진 설정 바꾸기
- `src/components/PhysicsCanvas.tsx`가 가장 중요한 파일입니다. 여기서 중력 크기(`gravity`), 공의 탄성(`restitution`), 마찰력(`friction`)을 조절할 수 있습니다.

### 성능 최적화 (Sleeping)
- 화면에 객체가 너무 많으면 버벅일 수 있습니다. Matter.js의 `enableSleeping` 옵션을 사용해 움직임이 멈춘 객체는 연산을 쉬게 하도록 설정되어 있습니다.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 화면이 멈추거나 엄청 느려져요.**
- A: 객체가 화면 밖으로 무한히 떨어지고 있거나 너무 많이 생성된 경우입니다. 일정 개수가 넘으면 오래된 객체를 자동으로 지우는 로직(`world.remove`)을 확인하세요.

**Q: 창 크기를 바꾸면 물리 세계가 깨져요.**
- A: 브라우저 창 크기에 따라 '물리 벽'의 위치도 같이 옮겨줘야 합니다. `resize` 이벤트 핸들러를 점검해 보세요.

---

## 📁 주요 구성 요소
- `src/components/PhysicsCanvas.tsx`: Matter.js 월드와 렌더링 로직이 담긴 핵심 컴포넌트.
- `src/utils/api.ts`: 실시간 시세를 가져와서 물리 엔진이 쓸 수 있게 가공하는 유틸리티.
